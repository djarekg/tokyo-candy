{
  "name": "@tc/workspace",
  "private": true,
  "version": "1.0.0",
  "packageManager": "bun@1.3.1",
  "engines": {
    "bun": "1.3.1"
  },
  "type": "module",
  "scripts": {
    "clean": " bunx rimraf -g **/node_modules **/bun.lock",
    "setup": "bun run clean && bun install"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "@types/bun": "1.3.1",
    "typescript": "^5"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  }
}
